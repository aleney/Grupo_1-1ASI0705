public class Rol {

    @Entity
    @Table(name = "Rol")
    public class Provider {
        @Id
        @GeneratedValue(strategy = GenerationType.IDENTITY)
        private int idRol;

        @Column(name = "tipo_usuar", length = 20, nullable = false)
        private String tipo_usuar;
    }
}
